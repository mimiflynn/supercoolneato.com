{
  "dependencies": {
    "moment": "^2.30.1",
    "typogr": "^0.6.8",
    "underscore": "^1.13.8",
    "wintersmith": "^2.5.0"
  },
  "private": "true",
  "scripts": {
    "clean": "rm -rf docs",
    "start": "wintersmith preview",
    "build": "npm run clean && wintersmith build && mv build docs && cp CNAME docs/CNAME"
  }
}
